<h1><%= link_to @song.title, song_path(@song) %></h1>

<%= link_to "All Songs", songs_path %>

<% if @song.artist %>
<h1><%=  link_to @song.artist.name, artist_path(@song.artist.id) %></h1>
<% else %>
<h1><%= link_to "Add Artist", edit_song_path(@song) %></h1>
<% end %>